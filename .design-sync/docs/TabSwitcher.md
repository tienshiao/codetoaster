---
category: Navigation
---

CodeToaster's per-session view switcher — terminal, diff, files, git. Reads the active session and route from context and navigates on selection, so it takes no props; render it inside the session layout rather than composing it standalone.
