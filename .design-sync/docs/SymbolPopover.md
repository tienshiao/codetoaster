---
category: Application
---

Code-intelligence popover shown when a symbol is clicked in a diff or file view. Pass the `sessionId` and a `target` describing the symbol (null closes it); it fetches definitions and references for that repo. `onClose` fires on dismiss.
