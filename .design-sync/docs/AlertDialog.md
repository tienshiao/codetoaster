---
category: Overlays
---

Confirmation dialog for destructive or irreversible actions. Unlike Dialog it is deliberately hard to dismiss: use `AlertDialogAction` and `AlertDialogCancel` in the footer rather than a close button. `AlertDialogMedia` is a CodeToaster addition for showing an image or diff excerpt above the message.
