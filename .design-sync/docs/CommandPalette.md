---
category: Application
---

CodeToaster's ⌘K palette. Wires the Command primitive to live sessions, projects, and app actions from context, so it takes no props and must render inside the session providers. It owns its own open state and keyboard shortcut.
