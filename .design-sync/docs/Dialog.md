---
category: Overlays
---

Modal dialog on Radix. Compose `Dialog` > `DialogTrigger` + `DialogContent`, and inside content use `DialogHeader` (`DialogTitle`, `DialogDescription`) and `DialogFooter` for actions. Always give a `DialogTitle` — it is the accessible name. Control it with `open`/`onOpenChange` when the trigger lives elsewhere.
