---
category: Forms
---

Single-line text field. A thin wrapper over `<input>`: every native prop passes through, and `className` merges via `cn()`. Focus and invalid states are token-driven (`ring`, `destructive`) rather than per-instance styling, so pair it with your own label and error text rather than restyling the control.
