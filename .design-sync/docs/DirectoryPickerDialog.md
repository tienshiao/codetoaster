---
category: Application
---

Modal filesystem browser for choosing a directory. Controlled with `open`/`onOpenChange`, starts at `initialPath`, and calls `onSelect` with the chosen absolute path. Used when creating a session or project.
