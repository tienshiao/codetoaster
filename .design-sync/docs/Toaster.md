---
category: Feedback
---

Global toast host (sonner), themed to match the design system. Mount it once near the app root, then call `toast(...)` from anywhere. It renders nothing until a toast fires.
