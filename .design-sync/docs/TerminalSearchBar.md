---
category: Terminal
---

Find bar for the terminal. Drives an xterm `SearchAddon` passed as `searchAddon`, with next/previous and match count; `onClose` hides it. Render it over the terminal viewport, not inside the xterm element.
