---
category: Overlays
---

Floating panel anchored to a trigger. `Popover` > `PopoverTrigger` + `PopoverContent`; `PopoverAnchor` positions against a different element than the trigger. For plain hover text prefer Tooltip; use Popover when the content is interactive.
