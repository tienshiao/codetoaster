---
category: Forms
---

Search/filter field used above lists. Controlled through `value` and `onChange` (which receives the string, not the event). Renders a leading search icon and a clear affordance once there is text, so it reads as a filter rather than a plain Input.
