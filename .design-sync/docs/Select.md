---
category: Forms
---

Dropdown selection built on Radix Select. Compose `Select` > `SelectTrigger` (with `SelectValue` for the current selection) plus `SelectContent` holding `SelectItem`s. `SelectGroup` with `SelectLabel` sections a long list, and `SelectSeparator` divides groups. Controlled via `value`/`onValueChange`.
