---
category: Layout
---

Show/hide a region. `Collapsible` > `CollapsibleTrigger` + `CollapsibleContent`, controlled with `open`/`onOpenChange` or left uncontrolled with `defaultOpen`. For many sibling sections, style the triggers consistently so the affordance reads as a group.
