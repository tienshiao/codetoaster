---
category: Application
---

Small single-field rename modal. Open it by passing a non-null `item` (`{id, name}`) — null keeps it closed — and give a `title` naming what is being renamed. `onRename` receives the id and the new name.
