---
category: Layout
---

Thin rule between content. `orientation` is `horizontal` (default) or `vertical`; a vertical separator needs a parent with a definite height. Decorative by default, so it is hidden from assistive tech.
