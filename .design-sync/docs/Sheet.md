---
category: Overlays
---

Panel that slides in from a screen edge. Same composition as Dialog (`SheetHeader`/`SheetTitle`/`SheetDescription`/`SheetFooter`); `SheetContent` takes `side` (`top`, `right`, `bottom`, `left`) to choose the edge. Use for secondary surfaces that should not take over the screen.
