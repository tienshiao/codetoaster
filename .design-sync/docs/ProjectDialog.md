---
category: Application
---

Create/edit form for a project. `mode` switches between the two; in `edit` mode pass the existing `project`. `onSave` receives the name and initial path, `onClose` handles both cancel and dismiss.
