---
category: Overlays
---

Short hover/focus hint. Wrap the app (or the subtree) in `TooltipProvider`, then `Tooltip` > `TooltipTrigger` + `TooltipContent`. Text only — anything interactive belongs in a Popover.
