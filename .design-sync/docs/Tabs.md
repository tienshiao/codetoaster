---
category: Navigation
---

Tabbed panels. `Tabs` (controlled by `value`/`onValueChange`) contains a `TabsList` of `TabsTrigger`s and matching `TabsContent` panels keyed by the same `value`. `tabsListVariants` exposes the list's styling recipe for custom tab bars.
